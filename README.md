# hitfactorpy_graphql_server

GraphQL API for managing match reports.

## Status

**Work in progress...**

```console
$ poetry install
$ uvicorn hitfactorpy_graphql_server.app:make_app --factory --reload
```
